tosca_definitions_version: dell_1_1

description: >
  This blueprint creates a Virtual Machine on a NativeEdge Endpoint and provides the VM login details.

imports:
  - dell/types/types.yaml
  - vm/definitions.yaml
  - vm/inputs.yaml
  - vm/outputs.yaml
  - multi-node/definitions.yaml
  - multi-node/inputs.yaml
  - multi-node/outputs.yaml


input_groups:
  artifact:
    display_label: Deployment Artifacts
    collapsible: true
    index: 0
    inputs:
      - edge_os_image_secret
      - os_type
  vm:
    display_label: Virtual Machine Configuration
    collapsible: true
    index: 1
    inputs:
      - vm_name
      - vm_hostname
      - vm_user_name
      - vm_password
      - vcpus
      - memory_size
      - os_disk_size
      - disk_controller
      - disk_wrapper
      - add_disks
      - additional_disks
      - vnic_0_segment_name
      - use_dhcp
      - static_ip
      - use_gateway
      - gateway
      - use_dns
      - dns
      - use_nat
      - port_forward_rules
      - add_nics
      - vm_add_nics

  multi_node:
    display_label: Additional Virtual Machines
    collapsible: true
    index: 2
    inputs:
      - deploy_additional_vms
      - child_blueprint_id
      - additional_server_count
      - additional_vm
      - add_vm_nics
      - add_vm_add_nics
      - add_vm_disks
      - add_vm_add_disks

  adv_opt:
    display_label: Advanced Options
    collapsible: true
    index: 3
    inputs:
      - hardware_options.vTPM
      - hardware_options.secure_boot
      - hardware_options.firmware_type
      - use_passthrough
      - pcie
      - usb
      - serial_port_wrapper
      - gpu
      - video

labels:
  csys-obj-type:
    values:
      - environment
  target_environment:
    values:
      - ned
  vendor:
    values:
      - tme-tech-ops
  solution:
    values:
      - edge-cloud-init
  version:
    values:
      - "4.1.0.0"

blueprint_labels:
  env:
    values:
      - NED
